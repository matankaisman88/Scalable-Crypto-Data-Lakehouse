# Scalable Crypto Data Lakehouse - Environment Overrides
# Copy to .env and adjust as needed. .env is gitignored.

# Optional: Limit fetch to specific symbol/date (passed to ingest service)
# SYMBOL=BTCUSDT
# DATE=2024-01-01

# Optional: Override config paths for local dev
# DATA_ROOT=./data
